# GitHub Pages serves public/ as-is and does not resolve Git LFS pointers, so APKs
# published here must stay normal git blobs or the download returns the pointer file.
*.apk -filter -diff -merge -text
